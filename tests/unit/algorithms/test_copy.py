"""Tests for pied-piper/algorithms/copy.py."""
import unittest
import pied_piper.algorithms.copy as copy
from unittest.mock import mock_open

class TestPiedPiper(unittest.TestCase):

    # Given a file, convertFile should convert to type byte, error if not
    # Given file and a byte from convertFile, revertFile should convert to type of file, error if not
    # Given a file path, algorithm(each of them) should work, error if not
    # Given a binary, metadata follows this format, error if not
    # Given a serial, json conversion exists, error if not
    # Given a json conversion, file should write, error if not
    # Given a file converted, file should be same type as initial file, error if not
    # Also have tests for null values

    def test(self): 
        pass

if __name__ == '__main__':
    unittest.main()      